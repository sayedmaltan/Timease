import 'package:flutter/material.dart';

class ContactsScreenViewBody extends StatelessWidget {
  const ContactsScreenViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
